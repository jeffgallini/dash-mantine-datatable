---
title: Fixed-height Scrolling
description: 'Constrain the table body to a fixed viewport while keeping the header and pagination stable.'
bread-crumbs: false
---
<!-- Generated by scripts/generate_recipes.py. Edit usage.py to change the code examples. -->

# Fixed-height Scrolling

Constrain the table body to a fixed viewport while keeping the header and pagination stable.

Source: `usage.py::build_scrolling_code(height=300)`.

```python
table = (
    dmdt.DataTable(
        id="scrolling-table",
        data=SCROLLING_RECORDS,
        columns=SCROLLING_COLUMNS,
        paginationMode="none",
    )
    .update_layout(
        radius="lg",
        withTableBorder=True,
        height=300,
    )
)
```
